#' função que obtém a previsão baseada no objeto train associado 
#' ao modelo usado. recebe também os dados de teste
#' o usuário pode escolher se deseja retorno do ranking de probabilidades
#' de acordo com a probabilidade escolhida ou se a probabilidade
#' associada a um ID passado como parâmetro
#' f_agbd_previsao 
f_agdb_previsao <- function () {
    # DESENVOLVER
}